# RoomLens

The project instructions live in `AGENTS.md`, which is shared by every agent
host used with this repo (Claude Code, Cursor, and Codex). Keeping one file
avoids the three copies drifting apart.

@AGENTS.md
